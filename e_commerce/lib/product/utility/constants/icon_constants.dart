import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AppIcon {
  static const Icon shop = Icon(Icons.shop);
  static const Icon explore = Icon(Icons.search);
  static const Icon cart = Icon(Icons.shopping_cart);
  static const Icon favorite = Icon(Icons.favorite_border);
  static const Icon account = Icon(Icons.account_circle_rounded);
  static const Icon search = Icon(Icons.search);
  static const Icon error=Icon(Icons.error);
}
